.row
  .span12
    .page-header
      %h1 Sign Up for Probabilistic Tipping

    = simple_form_for @user, html: { class: "form-horizontal" } do |f|
      = f.input :email
      = f.input :name
      = f.input :password
      = f.input :password_confirmation

      = f.error :base

      = f.button :submit, "Sign up"
